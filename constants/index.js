const USER_ROLES = {
  BASIC: 'basic',
  ADVANCED: 'advanced',
};

const JWT_SECRET = 'TOP_SECRET';

const ERROR_MESSAGES = {
  INVALID_CREDENTIALS: 'Email or password are incorrect',
};

module.exports = {
  USER_ROLES,
  JWT_SECRET,
  ERROR_MESSAGES,
};
